{% extends 'admin/base.html.twig' %}
{% block body %}
    <div class="container">

        <div class="content">
            <h1>Welcome to Admin Panel</h1>
            <br>
            <table class="table table-striped">
                <tbody>
                    {% for name,enntities in params %}
                    <tr>
                        <td>
                            {{ name }}
                        </td>
                        <td>
                            {{ enntities|length }}
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>

    </div>
{% endblock %}