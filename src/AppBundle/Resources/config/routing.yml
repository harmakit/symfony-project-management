app.default:
    path: /
    defaults: { _controller: AppBundle:Default:index }

app.workbench:
    resource: 'routes/workbench.yml'
    prefix:   workbench

app.auth:
    resource: 'routes/auth.yml'
    prefix:   /